name: CI
on:
  push:
  pull_request:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: repository structure
        run: |
          test -f README.md
          test -f LICENSE
          test -f SECURITY.md
          test -d src
